#include "main.h"
/**
 * _islower - is used to find a lower case character
 * @c: parameter
 * Return: returns 1 or 0 dependig on the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
