#include "main.h"

/**
 * _abs - return the absolute value of a number
 * @n: parameter
 * Return: returns the absolute value of the number n
 */
int _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n);
}
