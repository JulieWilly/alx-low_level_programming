#include "main.h"
#include <string.h>
/**
 * int _strlen - function used to get the lenght of a string
 * @s: parameters
 * Return: returns length of the string
 */
int _strlen(char *s)
{
	return(strlen(s));
}
	
